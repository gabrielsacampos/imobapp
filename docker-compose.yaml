version: "3"

services:
  postgres:
    image: postgres:latest
    container_name: pg
    env_file:
    - ./.env
    ports:
      - "5432:5432"
    volumes:
      - /Users/gabriel/Desktop/imobapp-volume:/var/lib/postgresql/data

