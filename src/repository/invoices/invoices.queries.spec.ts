import { getPaidInvoices, getImmutableInvoices, getOnlendings, getRevenue } from './invoices.queries';

describe('', () => {
  it('', async () => {
    const result = await getImmutableInvoices();
    console.log(result);
  });
});
